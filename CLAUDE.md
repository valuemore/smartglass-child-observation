# CLAUDE.md — 개발 가이드

이 파일은 Claude Code가 이 저장소에서 작업할 때 반드시 지켜야 할 기준을 정의합니다.

## 프로젝트 한 줄 정의

스마트안경(교사 1인칭 시점) 영상에서 유아 행동 관찰기록 **초안(후보)** 을 생성하는 연구용 웹 시스템.
AI는 후보만 제시하고 **교사가 최종 확정**한다.

## 절대 원칙 (위반 금지)

1. **영상 중심**: AI 분석의 중심 데이터는 교사 시점 영상이다. 오디오는 **보조 증거**일 뿐, 단독으로 관찰기록을 만들지 않는다.
2. **분석 방식 고정**: AI 분석은 **프레임 추출 + 비전 모델** 방식이다. 오디오 전사 중심으로 설계하지 않는다.
3. **AI는 확정하지 않는다**: 모든 관찰기록·영역·문항은 **후보**다. 교사가 검토·수정·확정한다.
4. **유아 자동 식별 금지**: AI는 `child_A`, `child_B` 등 **임시 ID만** 부여한다. 실제 가명 ID 매칭은 **교사**가 한다.
5. **분류 기준**: 1차 = 누리과정 5개 영역. **KICCE 유아관찰척도 60문항 매핑은 핵심 기능**(후보 제시).
6. **점수 금지**: 관찰수준 점수를 산출하지 않는다.
7. **개인정보 보호**: 원본 영상은 제한 접근·접근 로그·삭제 정책을 따른다. 외부 API에는 **선별 프레임/클립 + 필요한 텍스트만** 전송하고 **원본 영상은 전송하지 않는다.**

## 기술 스택 (1차 시연 확정)

- 언어/UI: **Python 3.11+ / Streamlit**
- 영상: OpenCV, ffmpeg, PySceneDetect
- 비전: 클라우드 멀티모달 LLM API (어댑터로 교체 가능)
- 오디오(보조): faster-whisper
- 저장소: **SQLite** + JSON/CSV (repository 패턴으로 추상화)
- 배포: **로컬 앱 + 클라우드 AI API**
- 입력 흐름: **녹화 영상 업로드 후 배치 분석**
- **Neo4j 사용 금지.** 향후 PostgreSQL/FastAPI/Next.js 확장을 위해 모듈을 분리한다.

## 모듈 구조

```
app/        Streamlit UI 전용 (도메인 로직 두지 말 것)
core/       도메인 모델·Pydantic 스키마·상수
services/   video / vision / audio / mapping / pipeline
storage/    repository 인터페이스 + sqlite 구현
resources/  nuri_areas.json, kicce_items.json
data/        영상·프레임·DB (git 제외, 제한 접근)
security/   접근 제어·감사 로그·삭제 정책
```

## 코딩·언어 규칙

- 사용자 대상 텍스트·주석·문서는 **한국어**로 작성한다.
- 도메인 로직은 UI(Streamlit)에 의존하지 않게 분리한다. UI 교체/추가가 쉬워야 한다.
- 외부 비전/STT 호출은 어댑터 인터페이스 뒤에 둔다(제공자 교체 가능).
- 비전 출력은 `core/schemas.py`의 Pydantic 모델로 **검증**한다.
- 비용 통제를 위해 프레임 수 상한을 설정에서 관리한다.

## 금지 사항

- AI가 관찰기록을 자동 확정하는 로직 추가 금지.
- AI가 유아 실명/가명을 자동 매칭하는 로직 추가 금지.
- 관찰수준 점수 산출 기능 추가 금지.
- 원본 영상 전체를 외부 API로 전송하는 코드 작성 금지.
- 비밀값(API 키)을 코드/저장소에 하드코딩 금지 (`.env` 사용, git 제외).

## 서브에이전트 활용 원칙

Claude Code는 작업 효율화를 위해 **서브에이전트를 적극 활용**한다.

| 상황 | 사용할 에이전트 |
|------|---------------|
| 파일 위치·심볼·패턴 탐색 | `Explore` |
| 구현 전 설계·아키텍처 검토 | `Plan` |
| Streamlit UI 변경 | `streamlit-ui-developer` |
| 영상·파이프라인 서비스 변경 | `pipeline-developer` |
| 누리/KICCE 매핑 변경 | `mapping-specialist` |
| 보안·개인정보 검토 | `security-privacy-reviewer` |
| pytest 실패·회귀 수정 | `test-debugger` |

- **탐색이 3회 이상 필요하면** Explore 에이전트에 위임한다.
- **독립 작업 여러 개는 병렬 실행**한다 (단일 메시지에 여러 Agent 호출).
- 에이전트 결과는 직접 검증(파일 확인·테스트 실행) 후 사용자에게 보고한다.

## 참고

자세한 내용은 `docs/` 디렉토리 문서를 따른다. 기준 충돌 시 이 파일과 `docs/00_DEVELOPMENT_PLAN.md`가 우선한다.
