# .env.example — 필요한 환경변수 샘플
# 실제 사용 시 .env 파일을 생성하고 아래 값들을 교체하세요.
# .env는 Git에 커밋하지 마세요.

# === 비전 LLM API 설정 ===
# VISION_PROVIDER: "mock" (기본, 외부 API 미사용) 또는 "external" (외부 API 사용)
VISION_PROVIDER=mock

# VISION_MODEL: 외부 모델 ID (VISION_PROVIDER=external일 때만 사용)
# 예: gpt-4o / gemini-1.5-pro / claude-sonnet-4-6
# VISION_MODEL=gpt-4o

# VISION_API_KEY: 외부 API 키 — 절대 실제 키를 이 파일에 넣지 마세요
# 실제 .env에만 설정하고 Git에 커밋하지 마세요
# VISION_API_KEY=your_api_key_here

# VISION_DRY_RUN: true이면 payload 검증만 하고 실제 외부 호출을 하지 않습니다 (기본: true)
VISION_DRY_RUN=true

# 프레임 수 / 크기 상한
VISION_MAX_FRAMES_PER_SEGMENT=4
VISION_MAX_IMAGE_BYTES=1048576
VISION_MAX_PAYLOAD_BYTES=20971520

# 외부 실호출 전 확인 단계 강제 여부 (기본: true — UI에서 확인 단계를 건너뛰지 않음)
VISION_EXTERNAL_CONFIRM_REQUIRED=true

# === 데이터 경로 ===
DATA_ROOT_PATH=./data
VIDEOS_DIR=./data/videos
FRAMES_DIR=./data/frames
AUDIO_DIR=./data/audio
DB_PATH=./data/app.db
EXPORTS_DIR=./data/exports

# === 리소스 경로 ===
RESOURCES_DIR=./resources
NURI_AREAS_JSON=./resources/nuri_areas.json
KICCE_ITEMS_JSON=./resources/kicce_items.json

# === 분석 파라미터 ===
# 프레임 추출
MAX_FRAMES_PER_VIDEO=30
MAX_FRAMES_PER_SEGMENT=5
BLUR_THRESHOLD=100.0

# 장면 분할
SCENE_DETECTOR=ContentDetector

# 오디오 STT (whisper 모델)
WHISPER_MODEL=base
STT_ENABLED=true

# === 비용 제어 ===
# 비전 API 호출 상한
MAX_API_CALLS_PER_VIDEO=50
MAX_TOKENS_PER_REQUEST=4000

# === 로깅 ===
LOG_LEVEL=INFO
LOG_FILE=./data/app.log

# === 개발 모드 ===
DEBUG_MODE=false

# === 감사 로그 행위자 ===
DEFAULT_ACTOR=teacher_demo
